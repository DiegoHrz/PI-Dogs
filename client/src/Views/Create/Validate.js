import React from "react";

const Validate = (state, name) => {
  switch (name) {
    case "name":
    case min_height:
    case max_height:
    case min_weight:
    case max_weight:
    case lifespan:
    case temperamentos:
    case raza:
  }
};

export default Validate;
